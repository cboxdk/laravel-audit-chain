---
title: Configuration
weight: 60
description: Every key in config/audit-chain.php and its environment variable
---

# Configuration

- **[Reference](reference.md)**: every key, its default and what it does.

Publish the file with `php artisan vendor:publish --tag=audit-chain-config`.
